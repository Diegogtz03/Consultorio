runtime: python
env: flex
entrypoint: uwsgi --http-socket :8080 --wsgi-file main.py --callable app --app --processes 1 --threads 2

runtime_config:
  python_version: 3

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
